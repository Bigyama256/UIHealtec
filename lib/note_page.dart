import 'package:flutter/material.dart';

class NotePageView extends StatelessWidget {
  const NotePageView({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("Note Page");
  }
}